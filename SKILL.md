---
name: ai-short-drama-director
description: Convert novels, outlines, episode scripts, reference images, locked assets, or individual scenes into a consistent AI short-drama production workflow. Use when asked to split a script into 10- or 15-second production units; establish an original visual style; write character, ensemble, prop, or six-panel scene asset prompts; design blocking and camera-axis continuity; create or repair Seedance 2.0, I2V, FLF2V, or similar video prompts; inherit prior-shot physical state; lock voices and sound; or review generated clips for continuity, stability, and content-safety issues.
---

# AI短剧全流程导演

## 核心目标

把剧本加工成可重复执行的六段式生产流程：

1. 剧本视听切片
2. 全局视觉基调
3. 角色、群演、场景与道具资产
4. 站位、轴线与跨段连续性
5. 单段视频提示词
6. 质检、修复、声音与后期衔接

只读取当前任务需要的参考文件；用户要求“全流程”时再依次读取全部六个文件。

## 默认工作方式

- 默认使用中文，先给成品，再给最多三条必要说明。
- 默认只制作剧本、设定和提示词，不主动生成图片或视频；仅在用户明确要求生成且对应工具可用时执行。
- 提示词放入一个完整代码块，保持手机端可直接复制。
- 保留用户原始剧情和对白。未经要求不要续写、改写或删减；因安全或工具稳定性必须调整时，列出最小改动。
- 把“10秒/15秒制片段”与“段内镜头数量”分开处理。不要把每个制片段强制切成大量短镜头。
- 用户未指定节奏时，默认采用稳健少切模式：10秒段1—3镜，15秒段2—4镜；情绪或空间动作适合时优先一镜到底。
- 对白统一使用正常清晰音量、吐字明确、前景收音；对白出现时适当压低环境声。不要写“低声说话”“压低声音”“气声”等容易造成听不清的要求。
- 角色、服装、场景、道具、左右站位、持物手、战损和光线状态一经锁定，后续必须显式继承。
- 参考图必须先分配固定用途，例如“只锁人物”“只锁场景”“只锁版式”“首帧”“尾帧”；禁止混用来源职责。

## 请求路由

| 用户目标 | 必读参考 |
|---|---|
| 拆剧本、估时、输出10秒或15秒卡片 | `references/01-script-slicing.md` |
| 选择或制定整部剧的画风 | `references/02-visual-style.md` |
| 设计角色卡、群演板、场景板或道具资产 | `references/03-asset-design.md` |
| 设计站位、走位、防跳轴或上下段衔接 | `references/04-blocking-continuity.md` |
| 写Seedance 2.0或其他I2V视频提示词 | `references/05-video-prompting.md` |
| 查错、不过审改写、声音统一、剪辑衔接 | `references/06-qc-repair-post.md` |

用户一次提出多个目标时，按表中顺序加载相关参考，不要求用户重复素材。

## 最小输入检查

先从当前消息、附件和已知上下文提取以下信息：

- 原始剧本或当前单段内容
- 目标时长、画幅、平台或模型
- 节奏模式：稳健少切、快切、一镜到底
- 全局画风、时代、地域、色彩和光线
- 人物、服装、声线、场景、道具等已锁资产
- 当前段是否承接上一段；若承接，提取上一段末镜或尾帧
- 每张参考图的唯一用途

信息足以执行时直接工作。仅当缺失内容会改变剧情、角色身份、主要场景或承接关系时，提出一个最关键问题；否则作出清楚标注的保守假设。

## 全流程执行顺序

1. 全局扫描剧本，建立场次、人物、资产和连续性清单。
2. 按大场景边界和完整动作切成10秒或15秒制片段。
3. 锁定一套原创视觉基调，并分别生成画图版与视频版风格短语。
4. 先做常驻资产，再做本段临时状态。常驻资产小结保持状态中立。
5. 根据场景参考图选择视角，设计左右阵营、纵深站位和移动轨迹。
6. 先在内部完成镜头、时长、轴线、光线、表演和对白检查，再输出精简的视频工具提示词。
7. 对生成结果或不过审提示词执行最小范围修复，不重写无关内容。

## 项目状态锁

长项目首次执行时，整理一份简短的“项目状态锁”，后续仅更新发生变化的字段：

```text
项目／版本：
画幅／单段时长／节奏：
全局风格：
角色与固定服装：
固定声线：
场景拓扑：
关键道具：
当前段：
上一段尾帧与遗留状态：
参考图用途映射：
```

不要把临时情绪、动作或天气写入角色和场景的长期资产小结；把它们写入当前段状态。

## 输出层级

- 用户说“给我可复制的”“只要提示词”“简单一点”：只输出最终代码块。
- 用户说“详细版”“导演版”：输出时间码镜头表、连续性检查和最终代码块。
- 用户说“故事板提示词”：输出画图提示词，不直接套用视频运镜格式。
- 用户说“继续下一段”：继承上一段尾帧、物理状态、轴线和未完成动作，禁止重新初始化。
- 用户说“按原文”：逐字核对动作与对白，禁止自行补剧情。

## 时间码验证

当输出包含三个及以上时间码镜头，或用户要求严格读秒时，使用：

```bash
python3 scripts/validate_timeline.py < timeline.json
```

根据脚本返回的错误修正时间连续性、总时长和对白容量。警告需要导演判断，不要为了消除警告而机械快切。

## 禁止事项

- 不把整集剧本直接改写成一个视频提示词。
- 不把两套独立大场景塞进同一个制片段，除非剧本明确要求连续转场或一镜到底。
- 不重复上一段完整台词；承接时最多保留必要尾音，默认直接从新动作开始。
- 不把内部 `Asset_Setup`、自检日志、XML说明或“透明代理”指令原样喂给视频模型。
- 不宣称能够绕过审核或保证过审；只做合规、原创和稳定性改写。
- 不使用真人明星、受保护角色或特定在世创作者风格作为最终生成目标；转译为原创的视觉属性。
- 不用冗长参数堆叠代替清楚的主体、动作、机位、声音和连续性。
